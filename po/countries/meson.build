# Install country translations
#
# Countries translations are not built by default, as they are only needed
#
i18n.gettext ('countries',
    args: [
        '--directory=' + meson.project_source_root(),
        '--from-code=UTF-8',
        '--no-location'
    ],
    install: false
)
